# SmartTools Tabs Importer

用于 Chrome / Edge 一键把当前打开的标签页导入 SmartTools 后台。

## 安装

1. 打开 Chrome / Edge 的扩展管理页。
2. 开启「开发者模式」。
3. 点击「加载已解压的扩展」。
4. 选择本目录：`extensions/open-tabs-importer`。

## 使用

1. 点击浏览器工具栏里的扩展图标。
2. 确认已登录 SmartTools；如果尚未打开后台，可让扩展自动打开后台页。
3. 确认「后台地址」是当前 SmartTools 后台地址。
4. 可点击「打开后台」进入 `/config.html`，或点击「打开主页」进入该站点根地址。
5. 在「发送标签到后台确认导入」中点击「收藏当前页」「当前窗口标签」或「所有窗口标签」。
6. SmartTools 后台会弹出确认界面。
7. 在后台确认界面里勾选需要导入的标签。
8. 先选择「所属分类」；该选择会通过 cookie 记住，下次自动使用。
9. 决定是「新建父卡片」还是「追加到已有父卡片」。
10. 如果新建，填写父卡片名称；如果追加，只能选择当前分类下已有的 expandable 父卡片。
11. 点击「确认导入」。
12. 回到 SmartTools 后台确认导入结果，然后点击「💾 保存」。

也可以不直接导入后台，改用以下方式导出当前打开的标签：

- 复制为 JSON 到剪切板。
- 复制为普通文本到剪切板；默认只复制 URL，取消「仅 URL」后复制标题和 URL。
- 下载为浏览器可导入的 HTML 书签文件。
- 下载为 SmartTools 可继续处理的 JSON 文件。

如果点击导入时后台没有打开，扩展会先保存待导入标签，再自动打开后台，并在后台界面准备好后弹出同一个导入确认界面。若尚未登录，请先登录；登录后如未自动弹出，可重新点击扩展导入。

## 行为说明

- 只导入普通网页标签：`http://` 和 `https://`。
- 「收藏当前页」只读取当前激活标签页；「当前窗口标签」和「所有窗口标签」保持批量导入行为。
- 不导入 SmartTools 后台页本身。
- 扩展只负责读取标签页并发送到 SmartTools 后台。
- 「打开主页」会从后台地址中只保留协议和域名，例如 `https://example.com/config.html` 会打开 `https://example.com/`。
- 导入结果可以是新建一个可展开父卡片，也可以把标签追加到当前分类下已有的 expandable 父卡片。
- 父卡片方式、父卡片名称和所属分类都在 SmartTools 后台确认界面设置。
- 所属分类会通过 cookie 持久化；如果未设置或分类不可用，自动导入未分类。
- 每个子卡片只保存标题、URL，以及非 base64 的 favicon 图片地址。
- base64 favicon、窗口/标签位置、激活状态等运行时信息不会保存。
